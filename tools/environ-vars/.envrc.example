export ENV=development
export NODE_ENV=${ENV}
export VITE_ENV=${NODE_ENV}
export IS_DEBUG=1

export FRONTEND_PORT=3000

export GRAPHQL_PORT=8080
export GRAPHQL_API=http://localhost:${GRAPHQL_PORT}/${ENV}
export VITE_GRAPHQL_API=${GRAPHQL_API}

export AWS_PROFILE=localhost-user
export AWS_ACCESS_KEY_ID=fakeMyKeyId
export AWS_SECRET_ACCESS_KEY=fakeSecretAccessKey
export AWS_REGION=us-east-1

export DB_PORT=8081
export DYNAMO_ENDPOINT=http://localhost:${DB_PORT}
